version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

dockers_v2:
  - image_templates:
      - ghcr.io/ibotta/passenger-datadog-monitor:{{ .Tag }}
      - ghcr.io/ibotta/passenger-datadog-monitor:latest
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - --platform=linux/amd64

archives:
  - formats:
      - tar.gz

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
