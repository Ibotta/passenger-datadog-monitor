version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - gocritic
    - revive
    - gosec

formatters:
  enable:
    - gofmt
    - goimports

  settings:
    revive:
      rules:
        - name: unused-parameter
          disabled: true
    gosec:
      excludes:
        - G204  # subprocess launched with variable (execCommand pattern is intentional)
